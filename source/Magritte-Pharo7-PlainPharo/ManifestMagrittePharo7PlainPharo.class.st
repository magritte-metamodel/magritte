"
I include code that should be loaded only in a Plain Pharo. 

In other words, if Magritte is loaded in Glamorous Toolkit, the package should not be loaded as the same code (e.g., methods) is already present in Glamorous Toolkit.
"
Class {
	#name : #ManifestMagrittePharo7PlainPharo,
	#superclass : #PackageManifest,
	#category : #'Magritte-Pharo7-PlainPharo-Manifest'
}
